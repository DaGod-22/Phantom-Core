import {NextResponse} from "next/server"; import {createUser,loginUser,clearSession} from "@/lib/auth"; export async function POST(req:Request){try{const b=await req.json();const u=b.mode==="signup"?await createUser(b.username,b.password):await loginUser(b.username,b.password);return NextResponse.json(u)}catch(e:any){return NextResponse.json({error:e.message||"Authentication failed"},{status:400})}} export async function DELETE(){await clearSession();return NextResponse.json({ok:true})}