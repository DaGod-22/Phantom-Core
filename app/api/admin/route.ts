import {NextResponse} from "next/server"; import {getSession} from "@/lib/auth"; import {sql} from "@/lib/db"; export async function POST(req:Request){const s=await getSession();if(!s?.isAdmin)return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.formData();const id=String(b.get("id"));const action=String(b.get("action"));if(action==="approve")await sql("update game_requests set status='approved' where id=$1",[id]);if(action==="block")await sql("update users set is_blocked=true where id=$1",[id]);if(action==="unblock")await sql("update users set is_blocked=false where id=$1",[id]);return NextResponse.redirect(new URL("/admin",req.url))}